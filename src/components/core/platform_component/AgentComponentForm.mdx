---
name: AgentComponentForm
menu: Core
route: /core/AgentComponentForm
---


import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'AgentComponentForm';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'slug', type: 'text' },
    { name: 'secret', type: 'password' },
    { name: 'changeSecret', type: 'checkbox' },
    { name: 'url', type: 'text' },
  ]}
  formOptions={[
    {
      name: 'slug',
      type: 'string',
      required: true,
      description:
        'The slug for the agent to be updated/created.'
    },
  ]}
  dataSources={[
    { name: 'agent', type: 'agent' },
  ]}
/>
