---
name: BridgeModelAttributeForm
menu: Core
route: /core/BridgeModelAttributeForm
---

import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'BridgeModelAttributeForm';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'name', type: 'text' },
    { name: 'mapping', type: 'code' },
  ]}
  formOptions={[
    {
      name: 'modelName',
      type: 'string',
      description:
        'When provided the form will be populated by retrieving the corresponding model and will update that model when submitted.',
    },
    {
      name: 'attributeName',
      type: 'string',
      description:
        'When provided the form will be populated by retrieving the corresponding attribute name and will update that attribute name when submitted. If not provided the form submission will create a new attribute.',
    },
  ]}
  dataSources={[
    { name: 'bridgeModel', type: 'BridgeModel' },
    { name: 'bridgeModelAttribute', type: 'bridgeModelAttributeMapping' },
  ]}
/>
