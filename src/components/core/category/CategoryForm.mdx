---
name: CategoryForm
menu: Core
route: /core/CategoryForm
---

import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'CategoryForm';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'name', type: 'text' },
    { name: 'slug', type: 'text' },
    { name: 'linked', type: 'checkbox' },
    { name: 'attributesMap', type: 'checkbox' },
  ]}
  formOptions={[
    {
      name: 'kappSlug',
      type: 'string',
      required: true,
      description: 'kapp slug containing the category',
    },
    {
      name: 'categorySlug',
      type: 'string',
      description:
          'When provided the form will be populated by retrieving the corresponding category and will update that category when submitted. If not provided the form submission will create a new category.',
    },
  ]}
  dataSources={[
    { name: 'attributeDefinitions', type: 'AttributeDefinition[]' },
    { name: 'category', type: 'category' },
  ]}
/>
