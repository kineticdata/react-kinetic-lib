---
name: Form Type
menu: Core
route: /core/formtype
---

import { Playground, Props } from 'docz';
import { FormTypeForm } from './FormTypeForm';
import { FormTypeTable } from './FormTypeTable';

## Form Type

### Props

<Props of={FormTypeForm} />

### Examples

#### Simple implementation of a `FormTypeForm`.

<Playground>
  <FormTypeForm kappSlug="services" />
</Playground>

Because no name was passed, submitting this form will create a new Form

#### Control the Layout of a `FormTypeForm`.

<Playground>
  <FormTypeForm kappSlug="services">
    {({ form }) => (
      <div>
        <h5>My Form Header</h5>
        {form}
      </div>
    )}
  </FormTypeForm>
</Playground>

Because no name was passed, submitting this form will create a new Space Attribute Definition

## Form Type Table

### Props

<Props of={FormTypeTable} />

### Examples

#### Simple implementation of a `FormTypeTable`.

Because no name was passed, submitting this form will create a new Form Type

#### Control the Layout of a `FormTypeTable`.

<Playground>
  <FormTypeTable columnSet={['name', 'slug']} kappSlug="services">
    {({ table }) => (
      <div>
        <h5>My Table Header</h5>
        {table}
      </div>
    )}
  </FormTypeTable>
</Playground>
