---
name: FormTypeFrom
menu: Core
route: /core/FormTypeFrom
---

import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'FormTypeFrom';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'name', type: 'text' },
  ]}
  formOptions={[
    {
      name: 'name',
      type: 'string',
      required: true,
      description:
        'The name for the form type to be updated/created.'
    },
    {
      name: 'kappSlug',
      type: 'string',
      description:
        'The slug of the kapp that does/will contain the form. Required if datastore form is false.'
    },
  ]}
  dataSources={[
    { name: 'formType', type: 'formType' },
  ]}
/>
