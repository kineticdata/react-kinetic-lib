---
name: IndexDefinitionForm
menu: Core
route: /core/IndexDefinitionForm
---

import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'IndexDefinitionForm';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'parts', type: 'select-multi' },
    { name: 'unique', type: 'checkbox' },
  ]}
  formOptions={[
    {
      name: 'indexName',
      type: 'string',
      required: true,
      description:
        'The name for the index to be updated/created.'
    },
    {
      name: 'formSlug',
      type: 'string',
      required: true,
      description:
        'The slug of the form that does/will contain the index.'
    },
  ]}
  dataSources={[
    { name: 'form', type: 'form' },
    { name: 'fields', type: 'fields[]' },
    { name: 'indexDefinition', type: 'indexDefinition' },
  ]}
/>
