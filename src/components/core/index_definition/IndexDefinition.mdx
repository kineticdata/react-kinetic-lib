---
name: Index Definition
menu: Core
route: /core/indexdefinition
---

import { Props } from 'docz';
import { IndexDefinitionForm } from './IndexDefinitionForm';
import { IndexDefinitionTable } from './IndexDefinitionTable';

## IndexDefinitions

### Props

<Props of={IndexDefinitionForm} />

### Examples

#### Simple implementation of a `IndexDefinitionForm`.

```javascript
<IndexDefinitionForm formSlug="alerts" />
```

#### Control the Layout of a `IndexDefinitionForm`.

```javascript
<IndexDefinitionForm formSlug="alerts">
  {({ form }) => (
    <div>
      <h5>My Form Header</h5>
      {form}
    </div>
  )}
</IndexDefinitionForm>
```

## Index Definition Table

### Props

<Props of={IndexDefinitionTable} />

### Examples

#### Simple implementation of a `IndexDefinitionTable`.

```javascript
<IndexDefinitionTable columnSet={['name', 'slug']} formSlug="alerts" />
```

#### Control the Layout of a `IndexDefinitionTable`.

```javascript
<IndexDefinitionTable columnSet={['name', 'slug']} formSlug="alerts">
  {({ table }) => (
    <div>
      <h5>My Table Header</h5>
      {table}
    </div>
  )}
</IndexDefinitionTable>
```
