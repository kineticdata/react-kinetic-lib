---
name: BridgeModelForm
menu: Core
route: /core/BridgeModelForm
---

import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'BridgeModelForm';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'name', type: 'text' },
    { name: 'status', type: 'radio' },
    { name: 'bridgeSlug', type: 'select' },
    { name: 'agentSlug', type: 'select' },
    { name: 'structure', type: 'text' },
  ]}
  formOptions={[
    {
      name: 'modelName',
      type: 'string',
      description:
        'When provided the form will be populated by retrieving the corresponding model and will update that model when submitted. If not provided the form submission will create a new model.',
    },
  ]}
  dataSources={[
    { name: 'model', type: 'BridgeModel' },
    { name: 'modelMapping', type: 'Mapping' },
    { name: 'agents', type: 'Agent[]' },
    { name: 'bridges', type: 'Bridge[]' },
  ]}
/>
