---
name: Kapp
menu: Core
route: /core/kapp
---

import { Props } from 'docz';
import { KappForm } from './KappForm';
import { KappTable } from './KappTable';

## Kapp Form

### Props

<Props of={KappForm} />

### Examples

#### Simple implementation of a `KappForm`.

```javascript
<KappForm kappSlug="services" />
```

#### Control the Layout of a `KappForm`.

```javascript
<KappForm>
  {({ form }) => (
    <div>
      <h5>My Form Header</h5>
      {form}
    </div>
  )}
</KappForm>
```

Because no name was passed, submitting this form will create a new Kapp

## Kapp Table

### Props

<Props of={KappTable} />

### Examples

#### Simple implementation of a `KappTable`.

```javascript
<KappTable columnSet={['name', 'slug']} />
```

#### Control the Layout of a `KappTable`.

```javascript
<KappTable columnSet={['name', 'slug']}>
  {({ table }) => (
    <div>
      <h5>My Table Header</h5>
      {table}
    </div>
  )}
</KappTable>
```
