---
name: BridgeModelQualificationForm
menu: Core
route: /core/BridgeModelQualificationForm
---

import { FormProps } from '../../../gatsby-theme-docz/components/FormProps';

export const name = 'BridgeModelQualificationForm';

<h2>{name}</h2>

<FormProps
  fields={[
    { name: 'name', type: 'text' },
    { name: 'resultType', type: 'select' },
    { name: 'query', type: 'code' },
  ]}
  formOptions={[
    {
      name: 'modelName',
      type: 'string',
      required: true,
      description:
        'The model name for the model the qualification is part of. ',
    },
    {
      name: 'qualificationName',
      type: 'string',
      description:
        'When provided the form will be populated by retrieving the corresponding qualification and will update that qualification when submitted. If not provided the form submission will create a new qualification.',
    },
  ]}
  dataSources={[
    { name: 'bridgeModel', type: 'BridgeModel' },
    { name: 'bridgeModelQualification', type: 'BridgeModelQualification' },
    { name: 'bridgeModelQualificationMapping', type: 'BridgeModelQualificationMapping' },
  ]}
/>
