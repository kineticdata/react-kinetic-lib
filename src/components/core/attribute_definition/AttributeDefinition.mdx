---
name: Attribute Definition
menu: Core
route: /core/attributedefinition
---

import { Playground, Props } from 'docz';
import { ColumnsTable } from '../../../docz/ColumnsTable';
import { AttributeDefinitionForm } from './AttributeDefinitionForm';
import { AttributeDefinitionTable, columns } from './AttributeDefinitionTable';

## Attribute Definition Form

### Props

<Props of={AttributeDefinitionForm} />

### Examples

#### Simple implementation of a `AttributeDefinitionForm`.

```javascript
<AttributeDefinitionForm attributeType="spaceAttributeDefinitions" />
```

Because no name was passed, submitting this form will create a new Space Attribute Definition

#### Control the Layout of a `AttributeDefinitionForm`.

```javascript
<AttributeDefinitionForm attributeType="spaceAttributeDefinitions">
  {({ form }) => (
    <div>
      <h5>My Form Header</h5>
      {form}
    </div>
  )}
</AttributeDefinitionForm>
```

Because no name was passed, submitting this form will create a new Space Attribute Definition

## Attribute Definition Table

### Props

<Props of={AttributeDefinitionTable} />

### Default Columns

<ColumnsTable columns={columns} />

### Examples

#### Simple implementation of a `AttributeDefinitionTable`.

Because no name was passed, submitting this form will create a new Space Attribute Definition

#### Control the Layout of a `AttributeDefinitionTable`.

<Playground>
  <AttributeDefinitionTable
    columnSet={['name', 'description', 'actions']}
    attributeType="spaceAttributeDefinitions"
  >
    {({ table }) => (
      <div>
        <h5>My Table Header</h5>
        {table}
      </div>
    )}
  </AttributeDefinitionTable>
</Playground>

Because no name was passed, submitting this form will create a new Space Attribute Definition
